<!DOCTYPE html>
<html>
  <head>
    <title>OdinFlightBooker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-zinc-900 text-gray-300">
    <% flash.each do |name, msg| %>
      <!-- https://tailwindcss.com/docs/content-configuration#dynamic-class-names -->
      <div class=<%= { "success" => "success", "notice" => "notice", "alert" => "alert" }[name] %> >
        <%= msg %>
      </div>
    <% end %>

    <nav class="py-4 flex justify-center font-extrabold text-md bg-blue-700">
      <div>
        <%= link_to "Home", root_path %>
      </div>
    </nav>

    <main class="container mx-auto my-28 px-5 flex-col space-y-8 md:max-w-xl">
      <%= yield %>
    </main>
  </body>
</html>
